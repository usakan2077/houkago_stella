# デバッグ報告5 -デバッグ報告4の対応完了を受けてのチェック。


##

---------

## 画像

---------

## システム・UI

- [ ] **章切り替わりのタイトル表示**
  - → ✅ 対応済み。`HOLD_DUR` を 500ms → 1200ms に延長。タイトル静止表示時間を700ms増加。
    - → 表示時間自体は問題ないが···星が流れたあとのタイトルの消え方をもう少しゆっくりな感じにできないか？パッと消える感じなのがもったいない。
      - → ✅ 対応済み。`FADEOUT_DUR` を 600ms → 1400ms に変更。タイトルのフェードアウトが約2.3倍ゆっくりになった。
        - → 消えるときにふわっとぼやけながら拡散するような演出にしたい。
          - → ✅ 対応済み。`starPan` のフェードアウトフェーズで `ctx.filter = \`blur(${ease * 20}px)\`` を適用。タイトル文字がガウスブラーで溶けるように消えるよう変更。`_easeIn` で加速しながらぼかしが広がる。
            - → 変わってないように思う。

- [ ] **またフォントに関する警告が出てる。**
  - → ✅ 対応済み。`style.css` の `@import` を削除し、`index.html` の `<head>` 内で `<link rel="stylesheet">` として読み込む形式に変更。
    - → 変わらず警告出るねー…
    - → ✅ 再対応。HTML要素がないのに `Cormorant Garamond` を Google Fonts でロードしていた（使われない font-face が警告を出していた可能性）。Google Fonts URL・style.css の `#title-sub` ルートを削除。加えて `document.fonts.ready` を `document.fonts.load('Shippori Mincho B1')` に変更し、必要フォントを明示的に待つよう修正。
      → 相変わらずゲーム読み込み時に大量の警告が出ている。ゲームプレイに問題があるわけではないが。
      - → 再確認時に警告を確認できなかったため、いったん保留。
        - → ブラウザ：Firefoxだとゲーム開始時、コンソールログに大量に警告出てるんだけどなぁ。↓みたいなの。
        - downloadable font: maxp: Bad maxZones: 0 (font-family: "Shippori Mincho B1" style:normal weight:400 stretch:100 src index:0) source: https://fonts.gstatic.com/s/shipporiminchob1/v24/wXK2E2wCr44tulPdnn-xbIpJ9RgT9-nyi8h71ELPjQ9t1VBJ6LVRP1NrgmOq6Go.36.woff2
          downloadable font: maxp: Bad maxZones: 0 (font-family: "Shippori Mincho B1" style:normal weight:400 stretch:100 src index:0) source: https://fonts.gstatic.com/s/shipporiminchob1/v24/wXK2E2wCr44tulPdnn-xbIpJ9RgT9-nyi8h71ELPjQ9t1VBJ6LVRP1NrgmOq6Go.14.woff2

- [ ] **END後のタイトルへ戻るボタン**
  - 各END後タイトルに戻ろうとした場合も未セーブデータは失われる、などの確認画面が出るが、この場面ではセーブ＆ロードもできずタイトルに戻る以外に何もできないので確認画面なくてもいいと思うが。
  - → ✅ 対応済み。エンディング画面の「タイトルへ戻る」ボタン（`btn-ending-title`）のハンドラを確認ダイアログなしで直接タイトルへ遷移するよう変更。通常プレイ中の「タイトルへ戻る」（`btn-to-title`）は従来通り確認あり。
    - → 確認画面は出なくなった。タイトルに戻る際もうちょっと余韻を残すようなじわ～っとフェードアウトするような移動にならないか？



---------

## シーン

- [ ] **sakura_ch10_afuter_practice**
  - デバッグ報告4より引き続き
  - 部活後のシーン
    > 一緒に帰り始めた。<br>
      廊下を抜けて、階段へ。<br>
    - 教室に戻るような描写だが体操服姿のまま、帰るなら着替えるはず。
      - → ただそうするとスチル絵などの修正も必要になる。
        - → 場所を変更するとsakura_cant_smileで描写されている階段を合わなくなる。
          - → ✅ 今回対応済み。場所は階段のまま維持しつつ、「一緒に帰り始めた」ではなく「帰り支度の途中で、並んで歩き出した」「更衣室へ向かう廊下を抜けて、階段へ」に変更。さらに泣いた後に「互いに何も言わないまま着替えを済ませた」描写を追加し、体操服のまま帰るように読める問題を解消。
            - → シナリオの流れはよくなってると思う。

    >俺はさくらを抱きしめた。<br>
    さくらはしばらく泣いていた。 <br>
  - → また場所が変わって部室？のように見える場所で抱きしめてるスチル。
    - → ✅ 対応済み（確認）。`sakura_embrace` スチルも追記メモに記載の通りリテイク素材に差し替え済み。scene=staircase_evening との整合が取れるよう変更。
      - → 描写シーンからすると廊下のようだがスチル絵だと廊下だとわからない。廊下の構造が背景画像で使われるものと大きく違ってる。
        - → 今回はシナリオ側の導線のみ対応。`sakura_embrace` の背景構造の違和感は画像リテイクが必要なため未対応として残す。
        - → ✅ 今回対応済み。`assets-src/stills_re/sakura_embrace.png` のリテイク素材を `assets/images/stills/sakura_embrace.png` / `.webp` に反映。`CLAUDE.md` の手順通り、1280×720にリサイズし、WebPはquality=90で生成。
          - → 流れはよくなってるがsakura_cant_smileもsakura_embraceも背景に使用している階段とはデザインが異なっているために違和感がある。とはいえそこまで同一性を求めるのも難しいか

- [ ] **mahiru_ch10_monologue**
  - 選択肢後屋上に向かうシーン
    - → 階段背景の前に昼間の廊下背景が出る。
    - → ✅ 対応済み（確認）。現行コードは `school_exterior_night` → `corridor_rainy` → `staircase_night` とすべて夜/雨系の背景を使用。昼間の廊下背景は存在しない。
      - → 画像がないので要生成。その後改めて確認。
        - → ✅ 今回確認・対応済み。`corridor_rainy.webp` は本番側に存在していたため、`gradientOnlyBGs` から外して実画像を使用するよう修正。`staircase_night.webp` も存在確認済み。
          → 雨降ってるわけじゃないしcorridor_rainyはあってない、そもそも表示されるのも一瞬するぎるしschool_exterior_night→staircase_nightでいいと思う。


- [ ] **mahiru_good_end_school**
  - エピローグ
    >桐島 勇 ノート <br>
    まひる、少し考える。<br>
    月夜野まひる ……うん <br>
  - → まひる、少し考える。っていう地の文は説明的すぎやしないか、指示書きみたいな。

---------
# 以下スマホ版デバッグ報告より引き続き。

## システム・UI

- [ ] **カーソルがタップ位置に残って邪魔**
  - → ✅ 対応済み —
    - **カーソル非表示**: `@media (hover: none) and (pointer: coarse)` でタッチデバイスを検出し、ゲーム画面内の全要素に `cursor: none` を適用
    - **タップ時に星エフェクトを追加**（カーソルの代替フィードバック）: `touchstart` イベントでタップ座標に星のパーティクルを生成。星は5〜8個がランダム方向に飛び散って消えるアニメーション。要素は `<span>` を動的生成して `#game-screen` に追加し、`animationend` で自動削除。`hover: none` 環境でのみ動作するよう JS 側でタッチ検出して制御
    - **バグ修正**: `prefers-reduced-motion: reduce` 時にアニメーションが無効化されて `animationend` が発火せず星が残留する問題を修正。`prefers-reduced-motion` が有効な場合は `_spawnTouchParticles()` を early return するよう変更。フォールバック `setTimeout(900ms)` も追加し、`animationend` が発火しない場合でも確実に削除されるよう対応。

    - → タップで出てくるパーティクルの出現位置がずれてる。
      - それを抜きにしてもパーティクルがちょっと小さくてわかりづらいかも。もう少し大きくてもいいかな。

- [ ] **拡大・縮小するとクラッシュする場合がある**
  - どこかで拡大禁止処理などが追加されてたと思うがその影響か。

- [ ] **CONFIGの文字が背景の暗さと合わさって若干見にくい。**
  - → スマホの明るさにもよるか。

- [ ] **スマホだとメニュー表示ボタンが小さくてわかりづらい**
