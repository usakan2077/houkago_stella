その他
画像読み込みのフォールバック？
→ ✅ 対応済み / プリロード時に解決済み拡張子を記録し、BG/スチルはフォールバックなしで即時適用。キャラは全PNG確定のため最初から .png 直接指定。404エラーを大幅削減。

フォントに対する警告が出てる。実害はなさそう
→ 未対応（実害なしのため保留）

[Engine] Loaded: scenarios/chapter1.md のようなデバッグ情報が常に出力される
→ ✅ 対応済み / 全 console.log/warn/error を VN_CONFIG.settings.debug フラグが true のときのみ出力するよう変更

<meta name="apple-mobile-web-app-capable" content="yes"> is deprecated の警告
→ ✅ 対応済み / mobile-web-app-capable を追加



システム・UI
重大事項：選択肢表示中にセーブ＆ロードできないのは致命的、最優先で修正すべし。
→ ✅ 対応済み (P0) / commit: 選択肢表示中のセーブ/ロードをガード

バグ：ロードするとBGMが再生されない。
→ ✅ 対応済み (P1) / セーブデータに currentBGM を保存し、ロード時に再生するよう修正

バグ：イベント画像表示中にセーブ、その後ロードしてもイベント絵が表示されす前後のシーン画像のまま進む。
→ ✅ 対応済み (P1) / セーブデータに currentStill を保存し、ロード時に復元するよう修正

バグ？：テキスト表示中にスキップを実行しても有効にはなってるが即実行はされず次のテキストとかスキップ開始。
→ 未対応 (仕様として許容範囲と判断)

バグ？：スキップ中にセーブやロード画面出せるけどスキップは継続されてる。
→ ✅ 対応済み (P3) / _openSaveLoad() でモーダルを開く前に skipMode を解除するよう修正

バグ？：ログ表示位置が最新位置に追従しない。前回の表示時のスクロール値を維持してるため。
→ ✅ 対応済み (P3) / requestAnimationFrame を使い DOM 反映後にスクロール最下部へ移動するよう修正

要望：confirmウインドウをゲームに合わせた装飾に。
→ ✅ 対応済み / window.confirm() をゲームデザインに合わせたカスタムダイアログに変更

要望：オートの速度設定とか。
→ ✅ 対応済み / メニューバーに CONFIG ボタンを追加。遅め/普通/速め/爆速から選択可能。設定はlocalStorageに保存

要望：スキップ中、画面クリックでもスキップ解除されないのは個人的に違和感
→ ✅ 対応済み / クリック時にskipModeを解除するよう修正

要望：スキップ/オートと同様にセーブ＆ロードもキーボード対応に。
→ ✅ 対応済み / F5=セーブ、F7=ロード のショートカットを追加

要望：セーブデータスロット数5個は少ない。
→ ✅ 対応済み / スロット数を 5 → 10 に拡張

要望：上書きセーブの確認ダイアログもあったほうが。
→ ✅ 対応済み / 既存スロットへのセーブ時にカスタム確認ダイアログを表示

情報：シーン切り替え時、初表示の背景だと読み込みにかかる時間で切り替えがワンテンポ遅れる、
      切り替えエフェクト→テキスト進む→背景切り替え、って感じ。
      表示済みの背景だとキャッシュが聞いてるのか特に違和感はない。
→ ✅ 対応済み / プリロード時に解決済み拡張子を記録し、_changeBackground で new Image()+onload を経由せず即時適用するよう修正。初回・2回目ともに同じタイミングで切り替わる。
情報：テキスト表示が常に1行単位なのが若干気になる。改行単位で次の表示になってしまってるというか。


シーン
cp1、転校生紹介後BGMがない
→ ✅ 対応済み (P1) / _gotoLabel() から _stopBGM() を削除し、選択肢ジャンプ後もBGMが継続するよう修正

cp2、最初の選択肢選択後BGMがない
→ ✅ 対応済み (P1) / 同上

cp2.昼食屋上、ことはずっと照れ顔なの違和感
→ ✅ 対応済み (P2) / 写真撮影の話が終わったタイミングで @expr kotoha normal を追加

cp2,図書館、ことはの表情指定甘い
→ ✅ 対応済み (P2) / 「小さく笑った」→ shy、「借りてみます」→ happy を追加

cp3、昼食屋上、カメラ撮影時のSEがセーブ画面を開いたときとかと同じなのはどうか？
→ 未対応

cp3、図書室、イベント絵の表示が短すぎる、表示時間をもう少し足すか表示した状態で少し会話進めてもいいかも
→ ✅ 対応済み (P2) / スチル表示中に地の文を移動、@wait 1800 で表示時間を延長

            同シーン、ことはいきなり泣き顔になるのは違和感、地の分でも触れないし。
→ ✅ 対応済み (P2) / 泣き顔直前に「ことはの目が、一瞬遠くなった。」の地の文を追加

            主人公がピアノをやってたのか？と尋ね昔は弾いていた、というくだりcp2でやってるよね
→ ✅ 対応済み (P2) / 重複会話を削除し、前章への参照セリフに差し替え

cp3、sakura、BGMがない
→ ✅ 対応済み (P1) / _gotoLabel() からの _stopBGM() 削除で解消

cp3,選択肢後BGMがない、最初の選択肢もそうだったから同様のバグかと
→ ✅ 対応済み (P1) / 同上
